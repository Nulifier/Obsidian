"use strict";

var getAll	= require("../lib/core/api/getAll");

describe("core.api.getAll", function() {
	beforeEach(function mockObsidian() {

	});

	it("should request the model from this.model");

	it("should capitalize the model name");

	it("should throw an error when the model doesn't exist");

	it("should return a response when the models are found");

	it("should return a properly formated JSend response");

	it("should pass an error to next when there is an error");
});